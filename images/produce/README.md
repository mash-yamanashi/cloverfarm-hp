# くろーばーふぁーむ山梨 HP（GitHub Pages 公開用）

## フォルダの中身

```
cloverfarm-hp/
├── index.html   … ページ本体
├── style.css    … デザイン（配色・レイアウト）
├── images/
│   ├── hero.jpg      … トップの写真（現在はロゴ代わりにも使用・仮）
│   └── produce/      … 生産物ごとの写真（7品目すべて掲載済み）
└── README.md
```

## GitHub Pagesでの公開方法（例）

1. GitHubで新しいリポジトリを作成します（個人アカウント配下）。
   - `ユーザー名.github.io` という名前にすると、そのユーザーの公開ルートとして使えます。
   - 別の名前（例：`cloverfarm-hp`）でも公開可能です（URLが `https://ユーザー名.github.io/cloverfarm-hp/` になります）。
2. このフォルダの中身（`index.html` / `style.css` / `images/` フォルダ）をリポジトリ直下にアップロードします。
3. GitHubのリポジトリ画面 → `Settings` → `Pages` を開き、公開元（Branch）を `main`（ルート）に設定します。
4. 数分後、指定のURLでサイトが公開されます。

## 現時点で「仮」になっている部分

- **ロゴ**：正式なロゴが未着手のため、いただいた写真をヘッダーの丸いロゴマークとして仮使用しています。正式ロゴが決まり次第、`images/hero.jpg` を差し替えるだけで反映できます。

### 生産物の写真の差し替え方（今後、新しい写真に更新する場合）

1. 新しい写真を `images/produce/` フォルダの該当ファイル（例：`eggplant.jpg`）に上書きするか、別名で保存して `index.html` 側の `src` を書き換えてください。
2. 正方形（1:1）にトリミングした写真だと、他の品目と統一感が出ます。

現在の掲載順・写真は、①とうもろこし ②ヤングコーン ③もも ④すもも ⑤黒ぶどう ⑥シャインマスカット ⑦なす、の7品目すべてご指定の順番で掲載済みです。

## 確認・ご相談したい点

- ヒーロー写真は横位置のトリミングです。別カットや縦位置の写真もあれば、構成の幅が広がります。
- SNSリンクは Instagram（@clover.farm_ym）とX（@clover_farm_ym）のみを設置しています。
- 公開時期は「でき次第」とのことでしたので、現時点ではスケジュールを設けていません。
